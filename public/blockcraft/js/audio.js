// STUB — silent sound engine (real WebAudio implementation replaces this).
export class SoundEngine {
  resume() {}
  setVolume(v) {}
  play(name, opts) {}
  playBlock(kind, group, pos) {}
  updateAmbient(state) {}
}
