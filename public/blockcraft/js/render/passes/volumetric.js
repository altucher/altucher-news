// STUB — to be replaced by the real VolumetricPass implementation (see ARCHITECTURE.md).

export class VolumetricPass {
  /** @param {import('../renderer.js').Renderer} renderer */
  constructor(renderer) {
    this.r = renderer;
  }
  onSettings(settings) {}
  resize(w, h) {}
  render(ctx) {}
  dispose() {}
}
