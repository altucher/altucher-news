// STUB — to be replaced by the real BloomPass implementation (see ARCHITECTURE.md).

export class BloomPass {
  /** @param {import('../renderer.js').Renderer} renderer */
  constructor(renderer) {
    this.r = renderer;
  }
  onSettings(settings) {}
  resize(w, h) {}
  render(ctx) {}
  dispose() {}
}
