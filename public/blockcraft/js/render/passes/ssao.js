// STUB — to be replaced by the real SSAOPass implementation (see ARCHITECTURE.md).

export class SSAOPass {
  /** @param {import('../renderer.js').Renderer} renderer */
  constructor(renderer) {
    this.r = renderer;
  }
  onSettings(settings) {}
  resize(w, h) {}
  render(ctx) {}
  dispose() {}
}
