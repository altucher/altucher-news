// STUB — to be replaced by the real ShadowPass implementation (see ARCHITECTURE.md).

export class ShadowPass {
  /** @param {import('../renderer.js').Renderer} renderer */
  constructor(renderer) {
    this.r = renderer;
  }
  onSettings(settings) {}
  resize(w, h) {}
  render(ctx) {}
  dispose() {}
}
