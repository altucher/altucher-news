// STUB — to be replaced by the real TAAPass implementation (see ARCHITECTURE.md).

export class TAAPass {
  /** @param {import('../renderer.js').Renderer} renderer */
  constructor(renderer) {
    this.r = renderer;
  }
  onSettings(settings) {}
  resize(w, h) {}
  render(ctx) {}
  dispose() {}
}
