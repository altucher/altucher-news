// STUB — to be replaced by the real CloudPass implementation (see ARCHITECTURE.md).

export class CloudPass {
  /** @param {import('../renderer.js').Renderer} renderer */
  constructor(renderer) {
    this.r = renderer;
  }
  onSettings(settings) {}
  resize(w, h) {}
  render(ctx) {}
  dispose() {}
}
