// STUB — to be replaced by the real SkyPass implementation (see ARCHITECTURE.md).

export class SkyPass {
  /** @param {import('../renderer.js').Renderer} renderer */
  constructor(renderer) {
    this.r = renderer;
  }
  onSettings(settings) {}
  resize(w, h) {}
  render(ctx) {}
  dispose() {}
}
